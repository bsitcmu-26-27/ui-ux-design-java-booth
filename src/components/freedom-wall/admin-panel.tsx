import { useState } from "react";
import { Check, RotateCcw, ShieldAlert, Trash2, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog";
import { categoryFor, type PostStatus } from "@/lib/freedom-wall";
import { useWall } from "./wall-provider";
export function AdminPanel() {
 const { posts, moderate, deletePost, resetDemo } = useWall(); const [filter, setFilter] = useState<"all" | PostStatus>("all"); const visible = posts.filter((p) => filter === "all" || p.status === filter);
 return <div className="mx-auto max-w-7xl px-5 py-10 md:px-8"><div className="mb-8 flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><Badge variant="outline" className="mb-3 gap-1 border-warning text-warning"><ShieldAlert className="size-3" /> Prototype only</Badge><h1 className="font-display text-4xl md:text-5xl">Moderation desk</h1><p className="mt-2 max-w-2xl text-muted-foreground">A local demo for reviewing wall content. This screen is not secured for production use.</p></div><Button variant="outline" onClick={resetDemo}><RotateCcw /> Restore sample wall</Button></div><div className="mb-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{(["all", "approved", "pending", "rejected"] as const).map((status) => <button key={status} onClick={() => setFilter(status)} className={`rounded-md border p-4 text-left transition-colors ${filter === status ? "border-primary bg-accent" : "bg-surface hover:bg-accent"}`}><span className="block text-2xl font-bold">{status === "all" ? posts.length : posts.filter((p) => p.status === status).length}</span><span className="text-xs capitalize text-muted-foreground">{status}</span></button>)}</div><div className="overflow-hidden rounded-md border bg-surface"><div className="hidden grid-cols-[1fr_150px_120px_180px] gap-4 border-b bg-muted/50 px-5 py-3 text-xs font-bold uppercase text-muted-foreground md:grid"><span>Note</span><span>Category</span><span>Status</span><span className="text-right">Actions</span></div>{visible.map((post) => <div key={post.id} className="grid gap-3 border-b px-5 py-5 last:border-0 md:grid-cols-[1fr_150px_120px_180px] md:items-center"><div><p className="line-clamp-2 text-sm font-medium">{post.message}</p><p className="mt-1 text-xs text-muted-foreground">{post.author} · {post.isSeed ? "Sample" : "Visitor post"}</p></div><span className="text-xs">{categoryFor(post.category).emoji} {categoryFor(post.category).label}</span><Badge variant={post.status === "approved" ? "default" : post.status === "rejected" ? "destructive" : "secondary"} className="w-fit capitalize">{post.status}</Badge><div className="flex justify-start gap-1 md:justify-end">{post.status !== "approved" && <Button size="icon" variant="ghost" onClick={() => moderate(post.id, "approved")} aria-label="Approve note"><Check /></Button>}{post.status !== "rejected" && <Button size="icon" variant="ghost" onClick={() => moderate(post.id, "rejected")} aria-label="Reject note"><X /></Button>}<AlertDialog><AlertDialogTrigger asChild><Button size="icon" variant="ghost" aria-label="Delete note"><Trash2 /></Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Delete this note?</AlertDialogTitle><AlertDialogDescription>This removes it from the prototype wall on this device.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={() => deletePost(post.id)} className="bg-destructive text-destructive-foreground">Delete</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div></div>)}{!visible.length && <p className="p-12 text-center text-muted-foreground">No posts in this queue.</p>}</div></div>;
}
