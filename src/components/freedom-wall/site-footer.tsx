import { Heart } from "lucide-react";
import { useRouterState } from "@tanstack/react-router";
export function SiteFooter() { const pathname = useRouterState({ select: (state) => state.location.pathname }); if (pathname === "/booth") return null; return <footer className="border-t border-border bg-primary py-10 text-primary-foreground"><div className="mx-auto flex max-w-7xl flex-col gap-5 px-5 text-sm md:flex-row md:items-end md:justify-between md:px-8"><div><p className="font-display text-xl">Freedom Wall</p><p className="mt-1 text-primary-foreground/65">A digital booth project by CMU IT students.</p></div><div className="max-w-xl text-primary-foreground/65"><p>This prototype stores posts only on this device. Don’t share personal or sensitive information.</p><p className="mt-2 flex items-center gap-1.5">Speak freely. React kindly. Keep the wall safe. <Heart className="size-3.5" /></p></div></div></footer>; }
